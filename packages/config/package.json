{
  "name": "@taskmanagement/config",
  "version": "1.0.0",
  "description": "Shared configuration modules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@taskmanagement/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "typescript": "^5.2.2",
    "vitest": "^1.0.0"
  }
}