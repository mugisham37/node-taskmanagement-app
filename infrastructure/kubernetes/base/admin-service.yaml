apiVersion: v1
kind: Service
metadata:
  name: taskmanagement-admin
  namespace: taskmanagement
  labels:
    app.kubernetes.io/component: admin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: admin