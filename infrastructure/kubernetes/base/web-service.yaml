apiVersion: v1
kind: Service
metadata:
  name: taskmanagement-web
  namespace: taskmanagement
  labels:
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: web